import styled from "styled-components";

export const HeaderContainer = styled.div`
  text-align: center;
  padding-top: 50px;
  text-transform: uppercase;
  font-size: .8rem;
  color: white;
  font-weight: 800;
  
  span {
    opacity: 90%;
    font-weight: 300;
  }
`